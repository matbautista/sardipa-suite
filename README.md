# Saripda Suite

A lead-to-sale tracking CRM for insurance agencies — leads, policies, renewals, and dashboards for Sales Agents, Sales Managers, and Agency Heads, self-hosted on a single office PC.

## Status

**All 17 phases from Section 10 of the plan are in place.** The full spec lives in [`insurance-crm-plan.md`](./insurance-crm-plan.md) (currently Baseline v1.4) and remains the source of truth for design rationale; this README documents what was actually built, phase by phase, including the real gaps and bugs each phase surfaced. For deploying this to an actual office PC, see [`DEPLOYMENT.md`](./DEPLOYMENT.md).

What phase 17 actually added:
- **`next.config.ts`**: `output: "standalone"` — Next.js traces exactly what `server.js` needs into `.next/standalone`, runnable with a plain `node server.js`, no `next` CLI or full `node_modules` required on the target machine (Section 8)
- **`scripts/package-release.mjs`** (`npm run package`) — builds and assembles the actual copy-paste deployment folder. Inspecting a real build's output (not just trusting the trace) turned up three things it silently gets wrong that this script fixes up: `better-sqlite3` is a native addon and the trace only copies its `package.json`, not the prebuilt binary `scripts/watchdog.mjs` needs (the Next.js server itself is fine — Prisma's adapter carries its own complete nested copy); the trace also swept this project's own `.env` (real dev secrets) and a stray dev-uploaded file from `./dev-storage` straight into the build output. All three fixed by the packaging script — the last two by deleting them and writing a fresh `.env` with a newly generated `AUTH_SECRET`, which is also where Section 7's "generated on first run" app-level encryption key actually happens for a packaged release. A fresh, empty, already-migrated `agency-crm.db` (via `prisma migrate deploy` against a temp `DATABASE_URL`, run on the build machine) means the packaged app boots straight to `/setup`, same as any real fresh install
- **`scripts/backup.mjs`** (`npm run backup`) — copies the database (via better-sqlite3's online backup API, safe while the app is live under WAL mode — not a raw file copy, which could grab a torn snapshot mid-write), `.env`, and every configured `StorageLocation`'s actual documents into a timestamped folder. Plain Node + `better-sqlite3` directly, no Prisma Client, same "must run standalone in the packaged folder" constraint as `watchdog.mjs`
- **A real, previously-invisible bug found via actual deployment testing, not just code review**: `scripts/watchdog.mjs` never loaded `.env` at all — `next dev`/`build`/`start` all load it internally, but a bare `node scripts/watchdog.mjs` doesn't get that for free. In dev this was invisible (the hardcoded default `DATABASE_URL`, `file:./dev.db`, happens to match dev's actual filename), but it broke silently in a packaged release (`agency-crm.db`, a different name): every `RestartEvent`/`SystemAlert` write watchdog attempts silently failed ("no such table") against a wrong, auto-created empty file — no crash, just restart history and the restart-loop alert never actually being recorded. Fixed with Node's built-in `process.loadEnvFile()` (no new dependency). Verified by actually renaming the failure into existence first (confirmed the bug), fixing it, then force-killing the supervised child process and confirming a real `RestartEvent` row landed in the right database afterward
- **Two more real gaps found while writing `DEPLOYMENT.md`** — describing the deployment story surfaced two things the plan calls for that no earlier phase actually built:
  - **System Configuration page** (`/admin/settings`, Super Admin) — Section 5 describes a settings page for the host's LAN IP/port so the app can "display/share the correct LAN URL," cross-referenced directly by Section 8's deployment guidance. Never built. Added now (`src/lib/system-config.ts`)
  - **"Add a new storage location" flow** — named explicitly in the plan's own phase-3 description ("also build the 'add another storage location' flow for when the first one fills up") but phase 3 only ever shipped `addInitialStorageLocation()`, which *rejects* a second location outright. Without this, the System Health page (phase 15) could tell you a drive was nearly full with no way to actually act on it except manually repointing the one path — which the plan explicitly says not to do, since it strands already-saved documents. Added `src/lib/storage-locations.ts` (`addStorageLocation`, transactionally deactivating the old active location and activating the new one, same partial-unique-index safety as the original) and its UI on the new System Configuration page
- Verified for real, end to end, not just read through: ran `npm run package`, copied the resulting `release/saripda-suite/` folder to a completely different filesystem path (simulating "another PC"), and started it from there via `start.sh` — confirmed `/api/health` returns real DB connectivity and `/` correctly redirects to `/setup` against the fresh empty database. Completed the actual first-run wizard against that copy using a synthetic second drive letter (Windows `subst`, same trick phase 3's own testing used), signed in, set the LAN IP on the new System Configuration page, force-killed the running server to trigger watchdog's crash-recovery path and confirmed both the restart and the correct `RestartEvent` row, then ran `npm run backup` against it and confirmed the database, `.env`, and a real test file placed in the storage location all landed in the backup folder correctly. Separately, in the normal dev environment, exercised the new "add storage location" flow end to end with a second synthetic drive — confirmed the old location correctly flipped to inactive, the new one active, and the action logged to the Activity Log with the actual actor and a description naming both locations

What phase 16 actually added:
- **A real gap found before building anything new**: Section 5 requires every create/update/delete on a Lead, Policy, or Document to be written to `ActivityLog` — but `leads.ts`, `documents.ts`, and `policies.ts`'s create/update paths never actually did this (only login and `policy_activated`/`policy_renewed` were logged). Backfilled all of it. `updateLead`/`deleteLead`/`reassignLead` gained an explicit `actorId` parameter alongside the existing (possibly-resolved-to-someone-else) `ownerId` — same "access check vs. attribution are two different things" split phase 12 already established for `activatePolicy`/`recordRenewal`/`uploadDocument` — so a Manager editing a teammate's record attributes correctly to the Manager, not the teammate. Automated writes (the daily renewal job, the mailbox poller) still don't log, for the same reason `renewal-job.ts` already documented: `ActivityLog.userId` is required and there's no real user to attribute an automatic change to
- **`/reminders`** — the full follow-up list; the dashboard widget only ever showed the top 5 (`getMyFollowUps()` extracted out of `getMyDashboardMetrics()` in `dashboard-metrics.ts` so both can share it)
- **`/search`** — text search across Leads and Policies by name/phone/email, scoped by role via the existing `getTeamMemberIds()` (agent = own records, manager = team, head = whole agency). `Policy` itself has no customer name/phone/email columns, so matching goes through whichever of its relations actually carries one — the originating `Lead` if it has one, or the category-specific detail table otherwise (`AutoOwner`, `TravelDetail`, Life's `Person` via `LifeInsured`/`LifeOwner`, `HealthDetail`, `PropertyDetail`'s address as the closest equivalent for a category with no personal-name field at all)
- **CSV export** (`/api/export/leads`, `/api/export/policies`, `?scope=team` for Manager/Head) — a small dependency-free `src/lib/csv.ts` (RFC 4180 quoting) rather than pulling in a package for what's a handful of fixed rules
- **Activity Log viewer**: `/agency/activity` (Agency Head, whole agency, filterable by user/date range, with a `?recordType=&recordId=` deep-link filter for a future "view history" link) and `/admin/activity` (Super Admin, host-level events only — `agencyId: null` — not every agency's log, which would break the tenant-isolation story the rest of the app maintains)
- Verified live: created a real lead through the UI, confirmed it appeared correctly in the Activity Log with the right actor and a link back to the lead; ran both CSV export scopes against real seeded data and checked the actual response headers/body; searched by a phone number and confirmed agency-wide results for the Head

What phase 15 actually added:
- **`src/lib/storage-health.ts`** — proactive disk-space/reachability monitoring per `StorageLocation` (Node's built-in `fs.statfs`, no new dependency), polled every 5 minutes like the other background jobs, raising/resolving `storage_full`/`storage_unreachable` `SystemAlert`s (since `SystemAlert` has no `storageLocationId` column, the location is identified by a parseable `[location:id]` prefix embedded in the message, stripped again for display)
- **Documents now write to a temp filename and rename on success** (`documents.ts`) — a failed or interrupted upload never leaves a partial file under the real name; disk-full/unreachable/generic failures return the exact wording from Section 9's failure-point table instead of throwing an unhandled error
- **`/admin/health`** — Super Admin System Health page: app uptime, active alerts (with a "mark resolved" action), storage locations with live free-space/reachability, and the last 10 restart events
- **`src/app/error.tsx` / `global-error.tsx`** — app-wide error boundaries. Closed a real gap: nothing previously caught an unhandled throw anywhere, so even the SQLite lock-timeout case `src/lib/prisma.ts` already documented ("the UI layer turns into 'The system is busy...'") had no UI layer to actually turn into that — it just crashed to Next's raw error screen
- Restart-loop protection and WAL/busy-timeout resilience were already fully built in phase 2 (`scripts/watchdog.mjs` already wrote `RestartEvent` rows and a `restart_loop` `SystemAlert`) — verified this before building anything new, so this phase only needed to give them their visible surface, per the plan's own framing of phase 15
- Verified live: logged in as Super Admin, confirmed `/admin/health` renders real data and its "Check now" button updates it end to end

What phase 14 actually added:
- **`src/lib/email-intake.ts`** — per-agency IMAP mailbox polling (`imapflow` + `mailparser`, new dependencies), every 5 minutes (boot + interval, same shape as the daily renewal job), parsing structured inquiry emails into unassigned Leads. The structured format itself (`Name:`/`Phone:`/`Email:`/`Insurance Line:`/`Message:`, one label per line) is this module's own definition, not something Section 12 had settled — the client-facing site that would actually send these emails isn't built yet, so the parser documents the format it expects rather than reverse-engineering a real one
- **`src/lib/crypto.ts`** — AES-256-GCM for `EmailIntakeConfig.encryptedPassword`, deriving the key from `AUTH_SECRET` via HKDF rather than introducing a second generated-on-first-run secret (and the `.env`-writing machinery that would need) — Section 7's own wording ("an app-level encryption key for the encrypted password *and* the session/auth secret") reads as one key serving both roles
- An email that doesn't match the expected format, or names a line the agency hasn't configured, still becomes a Lead — flagged `needs_review`, raw body kept in notes — never silently dropped, per Section 5
- **`/agency/email-intake`** (Agency Head) — mailbox settings, a status panel, and a manual "Poll now" button so a bad credential shows up immediately instead of after the next scheduled cycle. Connection failures write to `EmailIntakeConfig.lastErrorMessage` and raise a `SystemAlert`, shown right on this same page
- Verified live: composed a structured test email by hand, confirmed the parser produces a matched Lead with the right fields, and confirmed both the "doesn't match the format" and "line not found" fallbacks correctly set `needs_review`

What phase 13 actually added:
- **Recharts** is now a real dependency (Section 7's tech stack finally has its chart library wired up) — used only inside a "use client" component, since the rest of the app is server components by default
- **`/dashboard`** now shows every agency role's own numbers, not just a leads preview (Section 5's "Agent: my pipeline, my conversion rate, my sales this month, follow-ups due"): a 6-tile pipeline breakdown, conversion rate (won ÷ (won + lost), "—" when there's no closed lead yet), sales this month, and a follow-ups-due list merging Leads with a `nextFollowUpDate` and this agent's `grace_period` policies (using `gracePeriodEndsAt` as the due date) — sorted together, overdue items called out in red. Built for every agency role (Agent, Manager, Head), not gated to Agent, since a Manager/Head has their own leads/policies too, same reasoning `/leads` and `/policies` already apply
- **`/team/dashboard`** (Manager/Head only, same `requireManagerOrHeadSession()` gate as `/team/leads` and `/team/policies`) — the aggregate view from Section 5's other half: a leaderboard by agent (leads/won/conversion/active+ policies/premium/commission), conversion rate by insurance line, revenue & commission KPI tiles, a pipeline funnel, policies-by-status, and a trailing-6-month sales trend. Filters (date range, insurance line, agent, policy status) are a plain GET form, same pattern as the existing team pages
- **`src/lib/dashboard-metrics.ts`** — the query layer, split into `getMyDashboardMetrics` (own-records, every role) and `getTeamDashboardMetrics` (team-scoped, takes the already-resolved `ownerIds`/`members` from `team-access.ts` rather than re-deriving scope itself). "A sale counts the instant status reaches `active`" (Section 5) means sales-this-month and the monthly trend both key off `ActivityLog`'s `policy_activated` entries, not `Policy.startDate` — `Policy` has no `createdAt` of its own, and `startDate` can be backdated by the agent, so it isn't a reliable "when did this actually happen" signal the way an audit-log timestamp is
- **Revenue/commission totals default to "active status and later"** (every status except `draft`, per Section 5) but an explicit policy-status filter overrides that default, even to `draft` — consistent with how every other filter on the page behaves, and useful for seeing how much premium is sitting in undated draft policies
- **The trend chart deliberately ignores the date-range and status filters** — it already carries its own fixed trailing-6-month window, and a status filter has no sensible meaning against "activated this month" events. Documented inline and on the page itself, not a silent inconsistency
- **A real bug caught before it could ship**: `dashboard/page.tsx` originally wrote `const [leads, metrics] = !isSuperAdmin ? await Promise.all([...]) : [[], null]` — TypeScript couldn't unify the two branches' tuple types (the plain array literal has no relation to `Promise.all`'s inferred tuple type), so the build failed type-checking. Fixed by declaring `leads`/`metrics` with explicit types up front and conditionally assigning inside an `if`, rather than a ternary across mismatched literal and inferred types
- **A second real bug, this one caught by `npm run build`, not `npm run dev`** (the exact lesson phases 2 and 3 already learned the hard way, repeating here for chart code specifically): `charts.tsx` ("use client") originally imported its status-label constants from `dashboard-metrics.ts`, which pulls in `tenant-db.ts` → `prisma.ts` → `better-sqlite3` — a Node-only native module. Turbopack tried to bundle that whole chain for the browser and failed on `Module not found: Can't resolve 'fs'`. Fixed by splitting the plain label constants into their own import-free `src/lib/status-labels.ts`, safe for both server data-fetching code and client chart components to import from
- Chart colors and marks follow the project's data-viz conventions, not ad-hoc choices: sequential blue for plain magnitude comparisons (conversion-by-line, the trend lines), an ordinal blue ramp for the four in-progress pipeline stages with the reserved status palette for the Won/Lost binary outcome (green/red — a genuine status, not a further funnel stage), and the same reserved status palette for policies-by-status (draft = neutral gray, active/completed = good, grace_period = warning, lapsed = serious, cancelled = critical). Two separate single-series line charts for the monthly trend (sales count, premium) rather than one dual-axis chart, since the two measures are different units/scales
- Verified live against real data, not just by reading the code: inserted a throwaway set of won/lost leads, a follow-up-due lead, and policies at `active`/`grace_period`/`lapsed`/`draft` across several agents via a temporary script (not committed, not part of `seed.ts`), then fetched `/dashboard` and `/team/dashboard` through an authenticated session for both an Agent and the Agency Head. Confirmed the agent's own pipeline/conversion/sales-this-month/follow-ups numbers matched hand-computed expectations exactly, the leaderboard's per-agent premium/commission totals summed correctly to the KPI tiles, `lapsed` policies correctly counted toward "active and later" while `draft` didn't, and the policy-status filter correctly narrowed the KPI tiles (e.g. filtering to `lapsed` showed exactly that one agent's lapsed premium). Cleaned up afterward by deleting the throwaway script and re-running `npm run db:seed`, which returns `dev.db` to its normal idempotent state

What phase 12 actually added:
- **`src/lib/team-access.ts`** (new) — `getTeamMemberIds(agencyId, callerId, role)` (a Head's scope is the whole agency; a Manager's is themselves plus every agent with `managerId` pointing at them; an Agent's is just themselves) and `resolveAccessibleOwner(agencyId, callerId, role, recordOwnerId)` (returns the record's owner id if this caller may touch it, else `null`)
- **The "resolve access once, reuse everywhere" design decision**: rather than teaching every existing lib function (`leads.ts`, `policies.ts`, `documents.ts`, the five `*-details.ts` files) a second "caller vs. record owner" check, each detail page now resolves *which owner's records the caller may touch* once, up front, then calls the same "my own records" functions from phases 7–11 with that resolved owner id instead of always the caller's own — every one of those functions' existing `ownerId !== callerId`-shaped check still does real work, it's just checking against the record's actual owner rather than assuming it's always the caller. This kept the change surface small and left ~25 already-tested functions structurally untouched
- **A real bug caught before it could ship**: three functions (`activatePolicy`, `recordRenewal`, `uploadDocument`) use their `ownerId` parameter for *both* the access check *and* attribution (`ActivityLog.userId` / `Document.uploadedBy`). Blindly passing the resolved owner id into both roles would have silently misattributed a Manager/Head's own actions to the record's actual owner. Fixed by splitting each into two parameters — `ownerId` (access check, may be resolved to someone else) and `actorId` (always the real caller, never resolved) — before any of this shipped, not caught by live testing after the fact
- **`getDocumentForDownload`** is the one function that couldn't take a pre-resolved owner id like everywhere else — the download route only ever receives a bare `documentId`, with no `policyId` available to resolve access at the page level first — so it does its own internal resolution (fetch the document → derive the policy's owner → `resolveAccessibleOwner`) instead
- **A real bug found and fixed via live testing, before any commit**: a per-page `resolveOwnerOrRedirect()` helper, called from inside several `"use server"` actions on the same page but not itself marked `"use server"`, crashed every save on `/leads/[id]`, `/policies/[id]`, and all five policy detail sub-pages with *"Functions cannot be passed directly to Client Components unless you explicitly expose it by marking it with `use server`"*. A plain closure referenced from inside another server action isn't itself a valid, serializable server reference. Fixed by marking the helper `"use server"` too and having it independently call `requireAgencySession()` rather than accept a session object as a parameter — passing a NextAuth session as a bound arg to a server reference is exactly the kind of value that boundary isn't meant to carry
- **`/team/leads`** — the shared unassigned/needs-review queue (agency-wide for every Manager and the Head, not scoped to "own team," per Section 5) with a Claim action; a table-based leaderboard (counts by status per team member); a filterable full list with a per-lead Reassign control. **`/team/policies`** — same leaderboard/filter shape, no reassign (Section 3's permissions table only lists lead reassignment, not policy reassignment, as a capability)
- **`requireManagerOrHeadSession()`** added to `src/lib/session.ts`, and `src/proxy.ts` gained a `/team/*` gate — Manager and Head only, explicitly *not* Agent (unlike `/leads`/`/policies`, which are open to any agency role and only gate on having an `agencyId` at all)
- Verified live, not just by reading the code: signed in as each of Head/Manager/Agent in turn. Head claimed an unassigned lead and reassigned it; Manager opened an agent's Life policy through `/team/policies`, filled in Insured/Owner/Beneficiary on the `/life` sub-page, uploaded a Proof of Payment, and activated it — confirmed `Document.uploadedBy` and the `policy_activated` `ActivityLog` entry both correctly named the Manager (the real actor), while `Policy.ownerId` stayed the agent's (unchanged). Confirmed an Agent is bounced from `/team/*` both via the nav link (hidden) and a direct URL, and gets a 404 (not a redirect) hitting another agent's policy URL directly

**A full-app review after phase 11** (live walkthrough of every role + a static code-audit pass) found and fixed four issues, none shipped before this fix landed:
- **`proxy.ts` had no role gate for `/leads`/`/policies`**, unlike `/admin` and `/agency` which both redirect gracefully. A Super Admin (no `agencyId`) manually visiting either route passed proxy's blanket auth check, then hit the page's own `requireAgencySession()` throw — an unhandled-exception crash instead of a clean bounce to `/dashboard`. Reproduced live, fixed by adding the same redirect pattern the other two gates already use
- **`uploadDocument()` (`src/lib/documents.ts`) never verified policy ownership** — every other function touching a policy re-checks `.ownerId === callerId` before acting; this one only checked the policy existed in the caller's agency. Not exploitable in the shipped UI (the page that calls it already gates on ownership itself), but it broke the codebase's own convention with no comment explaining why. Fixed to match
- **`listPolicyDocuments` and the five `hasMinimum*Info` Activate-gate functions** trusted their caller to have already verified ownership rather than re-checking it themselves — thinner safety margin than the rest of this codebase's "re-verify inside every function that touches a policy" convention. Brought into line for consistency, even though no live bug depended on it
- **`prisma/seed.ts` was still missing `SystemAlert`/`EmailIntakeConfig`** from its cleanup order — the same shape of gap that hit phases 9, 10, and 11 in a row (a table gets real data before the seed script's cleanup order learns about it). Neither table has real data yet (phases 14/15, not built), so this was dormant, not yet a live bug — fixed pre-emptively rather than waiting for it to bite

What phase 11 actually added:
- **`/policies/[id]/auto`, `/travel`, `/property`, `/health`** — one requirement-checklist form per line category, each gating that category's Activate action the same way Life's does (phase 10)
- **A real, explicit scope decision, not silently invented content**: Section 11 marks Property and Health's *entire* field lists — and the extended Auto fields beyond the four already-confirmed ones (plate number, chassis number, OR/CR, etc.) — as "industry-typical drafts, not verified against this agency's actual policy documents or forms" (Section 12's open questions, still unresolved as of this plan version). Rather than either inventing more fields myself or blocking the whole phase on an answer only the agency can give, `PropertyDetail`/`HealthDetail` are modeled and built using exactly what Section 11 already drafted — nothing added, nothing guessed beyond it — with both the schema comments and the pages themselves flagging that these fields are provisional and may need a migration once the agency's real forms are checked against them
- Auto and Travel, by contrast, build on fields that were already either confirmed (Auto's core four) or already committed to the schema in phase 1 (`TravelDetail`) — not new content decisions, just UI for what already existed
- Health's Dependents field is deliberately free text, not a structured table like Beneficiaries — Beneficiaries has an explicit "up to 3/2" cap in the plan that justifies the fixed-slot UI pattern from phase 10; Dependents has no stated cap, so that pattern doesn't fit, and building an open-ended dynamic add/remove list would be real added complexity for a field list that isn't even confirmed yet
- Property's "Proof of Ownership" is a `Document` (new type `proof_of_ownership`), uploaded the same way as Proof of Payment — not a text field, consistent with how documents vs. data fields are already split everywhere else in this app
- **Another seed.ts gap of the same shape as phases 9 and 10**, caught and fixed before it could bite: the two new detail tables weren't in the cleanup order either

What phase 10 actually added:
- **`/policies/[id]/life`** (linked from a Life policy's own detail page) — Insured, Owner (with a "Same person as Insured" shortcut), and up to 3 Primary + 2 Contingent Beneficiaries, matching Section 11's full Life field list. Each of the three sections saves independently (its own form, its own button), on purpose: this is what lets an agent save partial progress on the long Life form across multiple sessions, per Section 5's stated reason for `draft` existing in the first place
- **"Same as Insured" is a real shared row, not a copied one** — `LifeOwner.personId` is set to the exact same `Person.id` as `LifeInsured.personId` when checked, so a later edit to the Insured's details is automatically reflected for the Owner too (`src/lib/life-details.ts`, matching the schema's own comment on this relationship). Unchecking it back to a distinct person creates a genuinely separate `Person` row rather than ever mutating the Insured's row through the Owner form
- **A real bug found and fixed the same session, before any commit**: the Owner form's personal-info fields (name, birthdate) had HTML `required` attributes copied straight from the Insured form — but those fields are supposed to be ignorable when "Same as Insured" is checked. The browser's native validation silently blocked the whole form from submitting whenever they were left blank, with no error message and no network request at all, making the "same as insured" shortcut completely unusable. Fixed by making `required` a prop on the shared `PersonFieldset` component, `true` for Insured and `false` for Owner — the server-side check in `upsertPerson()` still catches a genuinely incomplete non-same-as-insured Owner with a proper error message
- **Activate's minimum-required-info gate is now real for Life policies**, not just Proof of Payment (phase 9's placeholder): `hasMinimumLifeInfo()` requires an Insured, an Owner, and at least one Primary Beneficiary to exist before a Life policy can leave `draft`. Verified both directions live — a fully-filled-in policy activated successfully, and a bare one (Proof of Payment attached, nothing else) was correctly rejected with the exact error message naming what's missing
- Age is computed and displayed next to Birthdate, never stored — same "derive at render time, don't duplicate the source of truth" approach as elsewhere in this codebase
- Record locking (phase 8) covers this page too, sharing the same policy-level lock as `/policies/[id]` rather than a separate one — opening the Life details page for a policy someone else is editing renders it read-only exactly like the main policy page does

What phase 9 actually added:
- **Convert a Won lead into a policy** (`/policies/new?leadId=...`, reachable from a Won lead's own detail page) — creates a `draft` Policy with line/product pre-filled from the lead where set. Creation is lead-conversion only for this phase; the schema allows a policy with no `leadId` ("a walk-in renewal or a migrated legacy policy"), but building a UI for that wasn't part of this phase's explicit deliverable
- **Document upload & storage** (`src/lib/documents.ts`) — PDF/JPG/PNG up to 5MB, written to the active `StorageLocation` at `{agencyId}/{policyId}/{documentId}.{ext}` (the document's own UUID as the filename, never the uploaded name), served back only through an authenticated route (`/api/documents/[id]`) that re-checks agency + policy-ownership against the `Document` row before ever touching disk. Server Actions cap request bodies at 1MB by default — bumped to 6MB in `next.config.ts` to fit the plan's 5MB file limit with headroom. Client-side image compression (resize/compress before upload) is a recommendation in the plan, not a hard requirement, and isn't built — only the 5MB hard cap is enforced
- **The explicit Activate action** — gated on a Proof of Payment document being attached (`Policy.proofOfPaymentDocId`). The plan also gates this on the line's full minimum-required-info (Section 11), but that's the structured Life/Auto/Property/Health/Travel forms from phases 10-11, which don't exist yet — so Proof of Payment is the one requirement this phase can actually enforce; the code comments this explicitly rather than silently under-implementing the gate
- **The daily renewal/lapsing job** (`src/lib/renewal-job.ts`, scheduled from `src/instrumentation.ts` — Next.js's server-startup hook, since this app has no separate cron process) implements all three rules from Section 5's table: non-life/Term lapse immediately, non-term-traditional Life enters a 30-day `grace_period`, VUL never lapses off `renewalDate` at all, and Travel policies move straight to `completed` at their trip end date. Verified for real, not just by reading the code: seeded one test policy per category with a past-due `renewalDate`, restarted the server (which runs the job once at boot, not just on the 24h interval), and confirmed every one landed in the correct status — including the grace-period math (`gracePeriodEndsAt` = `renewalDate` + 30 days exactly) and the VUL policy being left untouched
- **The Renewal/Payment action** — requires a new Proof of Payment upload, then reactivates to `active` with the new `renewalDate` computed differently depending on origin: one year forward from the *original* `renewalDate` when coming from `grace_period` (so a mid-window payment doesn't shift the yearly anniversary), one year forward from *today* when coming from `lapsed`. Both branches verified live with distinct dates chosen specifically so the two computations couldn't coincidentally produce the same answer
- Record locking (phase 8) applies to Policies exactly like Leads — same `record-lock.ts`, just a different `recordType`. Verified live that a lock held by another user renders the policy read-only too, not just leads
- **A real gap this phase exposed and fixed**: `prisma/seed.ts` never had `policy.deleteMany()`/`document.deleteMany()` in its idempotent reset, since neither table had real data before this phase. Left as-is, a second `npm run db:seed` after any Policy existed would fail with a foreign-key violation trying to delete `Product`/`InsuranceLine` out from under it. Fixed — including nulling `Policy.proofOfPaymentDocId` first to break the `Policy`↔`Document` circular reference before either table can be cleared
- **Not built yet, deliberately**: `Policy.vulFundBalance` updates (Section 5 describes this as "a figure an agent/admin updates periodically," not a phase 9 checklist item — VUL policies just display whatever balance exists, with a note that periodic updates aren't wired up); a Cancel action (the `cancelled` status exists in the schema and the lapsing rules account for it, but no phase 9 deliverable asked for a UI to set it)

What phase 8 actually added:
- **Record locking** (`src/lib/record-lock.ts`) — opening a lead's edit page (`/leads/[id]`) checks it out; if someone else already holds it (and the lock isn't stale), the page renders read-only instead — a dimmed, genuinely non-interactive form (native `inert` attribute, not just `disabled` inputs) with a "Currently being edited by X, since TIME" notice, and the Delete control hidden. A lock auto-releases after 15 minutes of inactivity, and is explicitly checked in on Save or on the "Back to my leads" action (a server action now, not a plain link, specifically so it can check in before navigating away)
- **A real, deliberate scope decision, not an oversight**: the Manager/Head force-release override from Section 5 is *not* built yet, on purpose — there's no page yet where a Manager/Head can even reach another agent's lock to release it (that needs phase 12's cross-agent visibility), so a button for it would have no reachable target. `record-lock.ts`'s own comment explains this, and it's a clean, obvious addition once phase 12 lands
- Since only a lead's owner can reach its own edit page today (phase 7's scoping), there's no way to organically produce a same-lead two-different-people conflict yet either — verified the read-only branch and the stale-lock auto-release both work correctly anyway, by inserting/backdating a `RecordLock` row directly in the dev database and confirming the page reacted correctly, rather than skipping that test because the UI can't produce the scenario on its own yet
- Flagged for review, not silently decided: this phase's design leans on Phase 7 having scoped Leads CRUD to "owner only" for every role. Section 3 actually permits Manager/Head to edit *any* team/agency record, but that's gated behind phase 12's visibility work — see the phase 7 entry below for the reasoning already applied there

What phase 7 actually added:
- **`/leads`** and **`/leads/[id]`** — every agency role (Agent, Manager, Head) can add, edit, and delete their own leads and move them through the pipeline (`new → contacted → quoted → negotiating → won/lost`). Deliberately scoped to "my own leads" only for all three roles: Section 3's cross-agent visibility (a Manager seeing their team, a Head seeing the whole agency) is explicitly a later phase (12), not this one, so ownership here is always the calling user's own id regardless of role
- Picking a **Product** on the lead form derives its **Insurance line** automatically (`resolveLineAndProduct` in `src/lib/leads.ts`) rather than validating the two selects agree — same "derive, don't validate a redundant field" approach as phase 6's `insurance-lines.ts`
- Ownership is enforced on top of tenant-scoping, not instead of it: `getScopedPrisma` already confines every query to the caller's agency, and `getOwnLead`/`updateLead`/`deleteLead` add a second check (`lead.ownerId === callerId`) on top — verified live with two different agents, where one correctly got a 404 opening the other's lead by URL
- Found and fixed a bug the same day it shipped, before any commit: the dashboard's "your leads" preview and the `/leads` list both went stale after a `db:seed` reset while an old login session (JWT) was still active, because the session's `user.id` pointed at a cuid from the previous seed run that no longer existed. Not a code bug — a NextAuth JWT is a point-in-time snapshot (same fact already noted in phase 5 about `mustChangePassword`) — but worth knowing if leads seem to vanish after reseeding: sign out and back in for a fresh session bound to current data
- Applied last night's phase-6 lesson pre-emptively this time: both the line and product `<select>` elements on the edit form are keyed on their current value (`key={lead.lineId ?? "none"}` / `key={lead.productId ?? "none"}`), so a save-triggered `revalidatePath` re-initializes the visible selection instead of leaving React's uncontrolled-input state stale — the exact class of bug fixed in phase 6's manager-reassignment dropdown

What phase 6 actually added:
- **`/agency/lines`** (Agency Head only, enforced in `src/proxy.ts` and defense-in-depth via `requireHeadSession` in `src/lib/session.ts`) — configure the agency's own insurance lines (free-text `name`, fixed `category` the rest of the app's logic keys off of) and products under each line. `lifePolicyType` (term / non-term traditional / VUL) only applies to `category: "life"` lines and is silently dropped for others rather than erroring, since the form doesn't even show the field for them
- **`/agency/users`** — Agency Head creates Manager and Agent accounts (with agents optionally assigned a manager at creation or reassigned later), resets their passwords, and deactivates/reactivates them. Reuses the exact same one-time-temporary-password mechanism from phase 5's Super-Admin-creates-Head flow (`src/lib/password-policy.ts`), not a second one
- These two pages intentionally use two different form patterns for the same reason phase 5 picked `useActionState`: **insurance lines/products don't need to show a one-time secret**, so their forms are the plain server-action-in-a-Server-Component pattern (works without JS, redirects with an error in the query string on failure) — while **account creation and password reset do** need to display a secret inline without a redirect, so those stay `useActionState` client components, matching phase 5's form exactly
- Deactivate/reactivate and manager-reassignment are plain server actions too (`toggleActiveAction`, `reassignManagerAction` in `src/app/agency/users/actions.ts`) — no one-time secret involved, so no reason to reach for `useActionState` there either
- An Agency Head can never target themselves or another Head through any of these actions: `resetAgencyUserPassword`/`setAgencyUserActive` in `src/lib/agency-users.ts` only ever match `role === "manager" || "agent"`, which rules out self-lockout implicitly rather than needing an explicit "not yourself" check
- Prisma's generated `*CreateInput` types for tenant-scoped models (`InsuranceLine`, `Product`) require `agencyId` explicitly even though `getScopedPrisma`'s runtime extension always overwrites it anyway — see the comment in `src/lib/insurance-lines.ts` for why it's included redundantly rather than fought

What phase 5 actually added:
- **`/admin/agencies`** (Super Admin only, enforced in `src/proxy.ts`) — create an agency and its first Agency Head account together, atomically (`prisma.$transaction`, so a duplicate-email failure can't leave a headless agency behind — verified for real, not just by reading the code)
- **Temporary passwords** (`src/lib/password-policy.ts`): shown once on screen after creation, never persisted anywhere in plaintext beyond that response. A new `User.mustChangePassword` flag (migration `20260809134512`) forces the recipient to `/change-password` before anything else — same "block everything until this one thing is done" shape as the setup gate. After a successful change, the user is signed out and must log in again for a fresh JWT, rather than trying to patch the already-issued session token in place
- The create-agency form uses React 19's `useActionState` (a client component) instead of the plain server-action-in-a-Server-Component pattern the other forms use, since it needs to show the one-time temporary password inline without a redirect that would expose it in a URL. Worth knowing if you touch this form: it needs real JS/hydration to work at all (renders with an empty `action=""` without it, unlike the progressive-enhancement fallback the other forms get for free), and React resets all the fields after any submission — including a failed one — which is default framework behavior, not something this code opted into

What phase 3 actually added:
- **`/setup`** — blocks every route (via `src/proxy.ts`) until it's done: create the Super Admin account, then choose a document storage location on a different physical disk than the app. Once both exist, `SystemConfig.setupCompletedAt` is set and `/setup` redirects to `/login` from then on (re-checked, cached in memory after the first `true` so it isn't a DB hit on every request forever)
- **The "different disk" check** (`src/lib/storage-path.ts`) — Windows compares drive letters (a UNC/network path always passes automatically, since it can never be the same physical disk), POSIX compares the filesystem device id. Tested for real, not just by reading the code: created a synthetic second drive letter with Windows' `subst` to force a genuine cross-drive comparison, confirmed the same-disk case is correctly rejected and the different-disk case correctly succeeds (directory gets created and write-tested), and confirmed the hint text correctly names the app's actual drive
- Building this exposed that `auth` from NextAuth isn't callable as `auth(request)` directly — it only works that way when exported as *the* proxy function with nothing wrapping it. Needing the setup-gate to run first meant wrapping a custom callback (`auth((req) => ...)`) instead, which bypasses `authConfig`'s `authorized` callback entirely — so the login-redirect logic that used to live there now lives directly in `proxy.ts` instead
- The seed script now also completes setup (a local `./dev-storage` folder, no real disk-separation needed for dev data) — otherwise every fresh `npm run dev` would land on `/setup` instead of `/login`, contradicting this README
- Found two real bugs by testing beyond dev mode, one of them severe:
  - **Login was completely broken under `next start` (production mode)** — every credentials login failed with a 500 (`UntrustedHost`). NextAuth requires either a trusted-platform host header (Vercel, etc.) or an explicit opt-in; this app is always self-hosted, so it never had either. `npm run dev` never surfaced this at all — this is a repeat of the exact lesson from `proxy.ts`'s Next.js 16 rename: **`npm run build` + `npm run start` is required to trust that a change actually works**, not just `npm run dev`. Fixed with `trustHost: true` in `src/auth.config.ts`.
  - **A genuine race condition** in `addInitialStorageLocation` (check-then-act on "is there already an active storage location," no transaction between the two) — reproduced it for real with two genuinely concurrent requests against two synthetic Windows drives, though the first attempt didn't happen to trigger it (lucky timing, not safety). Closed at the database level with a partial unique index (`StorageLocation(isActive) WHERE isActive = 1`, migration `20260809055955`) rather than relying on the application-level check, then re-verified under real concurrency that it now fails safely with a friendly error instead of either duplicating the row or throwing an unhandled 500

What phase 2 actually added:
- **WAL mode**, set once via a migration since (unlike `busy_timeout`) it's a durable property of the database file itself, not a per-connection setting
- **`busy_timeout` set explicitly** (5000ms) on the SQLite driver adapter
- **Retry-with-backoff on top of that** (`src/lib/prisma.ts`) — verified empirically that SQLite lock contention surfaces through this Prisma/adapter combination as error code `P1008` ("Operation has timed out"), not a raw `SQLITE_BUSY`; 3 attempts with short backoff between them, tested by actually holding a write lock across process boundaries long enough to force real contention, both the success and exhausted-retries cases
- **`GET /api/health`** — public (excluded from the auth gate in `src/proxy.ts`), checks real DB connectivity
- **`scripts/watchdog.mjs`** — the process supervisor: polls `/api/health`, restarts the app on a sustained failure window or an outright crash, and stops auto-restarting (writing a `SystemAlert`) after repeated restarts in a short window. Run via `npm run start:supervised`. Verified live, not just read through: crash detection, the health-check-failure path, restart-loop protection triggering at the configured threshold, and — this caught a real bug — a race condition where killing a child during a deliberate restart also fired the original "unexpected exit" handler, triggering a second overlapping restart and `EADDRINUSE` chaos. Fixed and re-verified. See the file's own header comment for a known Windows limitation around how it must be stopped to avoid orphaning the app process.

What phase 4 actually added:
- **Auth.js (NextAuth v5)** with a Credentials provider — email/password checked against `User.passwordHash` (bcrypt)
- **Login security baseline** (Section 5): 10-char minimum enforced wherever passwords are set (not yet — there's no set-password UI yet, only the seed script), and lockout after 5 consecutive failed attempts for 15 minutes, tracked on `User.failedLoginAttempts`/`lockedUntil`
- Every login attempt (success, failure, or attempted-while-locked) is written to `ActivityLog`
- **Session carries `agencyId` and `role`** (Section 7), via JWT so `src/proxy.ts` can check it without a DB call on every request
- **`src/proxy.ts`** blocks every route except `/login` for unauthenticated requests (this is Next.js 16's renamed `middleware.ts` convention — see the note below)
- **`src/lib/tenant-db.ts`** — the centralized tenant-scoping data-access layer Section 2 asks for: `getScopedPrisma(agencyId)` returns a Prisma Client where every operation on a tenant-scoped model is confined to that one agency automatically. Single-record operations (`findUnique`/`update`/`delete`) verify ownership before running, not after, so a cross-tenant id is never fetched, updated, or deleted — see the comment at the top of that file for the exact mechanism and its one real caveat (nested relation writes aren't auto-scoped)
- A minimal `/dashboard` page proving it end-to-end: Super Admin sees every agency; an agency user sees only their own agency's leads, fetched through the scoped client

A few items in the plan's Section 12 still need confirmation before or during the build (draft insurance-line field lists, the legal data-retention period, and some external logistics like drive sourcing and trademark clearance) — see that section for the current list.

**Note on the Next.js version**: this scaffold used Next.js 16, which is new enough that its own generated `AGENTS.md` warns that App Router conventions may differ from older training data. Before writing route/page code, check `node_modules/next/dist/docs/` rather than assuming Next 13–15 patterns still apply. Concretely: Next.js 16 renamed the `middleware.ts` file convention to `proxy.ts` (default export or a named `proxy` export required — `npm run dev` didn't catch this, only `npm run build` did) and changed its default runtime from Edge to Node.js. **Always run a production build, not just `npm run dev`, before calling a change done** — dev mode silently tolerated the old `middleware.ts` file with no warning.

## Tech stack (as actually scaffolded)

- **Frontend**: Next.js 16 (App Router) + TypeScript + Tailwind CSS v4
- **Database**: SQLite via Prisma ORM 7, using the `@prisma/adapter-better-sqlite3` driver adapter (required in Prisma 7 — there's no more built-in native engine binary)
- **Password hashing**: bcryptjs
- **Auth**: Auth.js (`next-auth@beta`, v5) — Credentials provider, JWT sessions
- **Charts**: Recharts, used from a single "use client" component (`src/app/team/dashboard/charts.tsx`) — the rest of the app stays server components by default
- **Email intake**: `imapflow` (IMAP client) + `mailparser` (message parsing), polled from `src/instrumentation.ts`

SQLite has no native enum type, so every "enum-like" column (`role`, `status`, `category`, etc.) is a plain `String` in `prisma/schema.prisma`, with the allowed values documented in a comment above each field. Validation happens at the application layer once that code exists.

## Setup

1. Install [Node.js](https://nodejs.org/) 22+ (this project was scaffolded on Node 24)
2. Clone the repo and install dependencies:
   ```sh
   git clone https://github.com/matbautista/saripda-suite.git
   cd saripda-suite
   npm install
   ```
3. Copy the environment file and generate a real `AUTH_SECRET`:
   ```sh
   cp .env.example .env
   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
   # paste the output as AUTH_SECRET in .env
   ```
4. Apply the database schema and seed some sample data:
   ```sh
   npx prisma migrate dev
   npm run db:seed
   ```
   This creates `dev.db` (gitignored) with a Super Admin, two sample agencies (each with a Head, a Manager, agents assigned to that Manager, some products, and a few leads). The seed script prints the shared dev password (`ChangeMe123!`) for every seeded account — every account uses the same one.
5. Start the dev server:
   ```sh
   npm run dev
   ```
   App runs at http://localhost:3000 and immediately redirects to `/login`. Sign in as e.g. `bea.santos@sunriseinsuranceagency.dev` (an agent) or `superadmin@saripda.dev` (Super Admin) to see the difference in `/dashboard`. Emails follow `firstname.lastname@<agency-name-no-spaces>.dev` — check `prisma/seed.ts` or `npm run db:studio` for the full list.

### Useful scripts

| Command | What it does |
|---|---|
| `npm run dev` | Start the Next.js dev server |
| `npm run start:supervised` | Run under the process supervisor (`scripts/watchdog.mjs`) — requires `npm run build` first, same as plain `npm run start`. For testing production-like reliability behavior, not everyday dev |
| `npm run db:migrate` | Create/apply a Prisma migration |
| `npm run db:seed` | Re-run the seed script |
| `npm run db:studio` | Open Prisma Studio (database GUI) |
| `npm run lint` | ESLint |
| `npm run package` | Build and assemble the self-contained deployment folder (`release/saripda-suite/`) — see `DEPLOYMENT.md` |
| `npm run backup` | Back up the database, `.env`, and every storage location's documents — `node scripts/backup.mjs <destination>` |

A genuinely fresh install (no seed data) lands on `/setup` first — see "What phase 3 actually added" above.

For production deployment (self-hosted, LAN-only, copy-paste portable), see **`DEPLOYMENT.md`** and Section 8 of the plan.

## Contributing

This is a single-agency internal tool, not an open-source project — see the plan document for design rationale before making changes.
